# Rust build output, for both the kernel and the userspace crate
target/

# Flattened kernel image produced by run.sh
*.bin

# Disk image run.sh creates for the block-device test
*.img

.idea/
.vscode/
